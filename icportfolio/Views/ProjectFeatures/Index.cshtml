@model IEnumerable<icportfolio.ProjectFeature>

@{
    ViewBag.Title = "Index";
}

<h2>Index</h2>

<p>
    @Html.ActionLink("Create New", "Create")
</p>
<table class="table">
    <tr>
        <th>
            @Html.DisplayNameFor(model => model.Project.name)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.Property.Property1)
        </th>
        <th></th>
    </tr>

@foreach (var item in Model) {
    <tr>
        <td>
            @Html.DisplayFor(modelItem => item.Project.name)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.Property.Property1)
        </td>
        <td>
            @Html.ActionLink("Edit", "Edit", new { id=item.ProjectFeatureID }) |
            @Html.ActionLink("Details", "Details", new { id=item.ProjectFeatureID }) |
            @Html.ActionLink("Delete", "Delete", new { id=item.ProjectFeatureID })
        </td>
    </tr>
}

</table>
